# vPay 环境变量配置
# 复制此文件为 .env 并填入实际值
# 优先级：环境变量 > .env 文件 > config/database.php 默认值

VPAY_DB_HOST=127.0.0.1
VPAY_DB_PORT=3306
VPAY_DB_NAME=vpay
VPAY_DB_USER=vpay
VPAY_DB_PASS=vpay@2085

# 站点基础URL（推荐设置，防止Host头注入）
# 不设置则自动使用 SERVER_NAME
# VPAY_BASE_URL=https://your-domain.com
